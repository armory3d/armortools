# amake

armorcore build tool.

**Windows (x64)**
```bash
../../make --compile --alangjs
```

**Linux (x64)**
```bash
../../make --compile --alangjs --ccompiler gcc --cppcompiler g++
# (gcc has lto enabled)
```

**macOS (arm64)**
```bash
../../make --compile --alangjs
```
